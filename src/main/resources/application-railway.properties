# ============================================================
# PROFIL RAILWAY — override application.properties
# Aktif saat Dockerfile ENTRYPOINT set -Dspring.profiles.active=railway
# ============================================================

# Supabase PostgreSQL
spring.datasource.url=jdbc:postgresql://db.zkuxzgpaocdzruldzdqj.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=jejakilmu123
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings untuk Supabase
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.ssl=true
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslmode=require

# H2 Console nonaktif
spring.h2.console.enabled=false

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
