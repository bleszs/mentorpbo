# ============================================================
# PROFIL RAILWAY — override application.properties
# Aktif saat Dockerfile ENTRYPOINT set -Dspring.profiles.active=railway
# ============================================================

# Supabase PostgreSQL — Transaction Pooler (port 6543, IPv4, reachable from Railway)
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.zkuxzgpaocdzruldzdqj
spring.datasource.password=jejakilmu123
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool — transaction mode pooler (stateless, max 3 connections)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.data-source-properties.sslmode=require

# H2 Console nonaktif
spring.h2.console.enabled=false

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
